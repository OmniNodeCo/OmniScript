# How to use

Start with [[Install]], then [[Four-Commands]]:

```python
draw_gui.window_size(420, 260, "My first GUI")
draw_gui.button(pos=(16, 60), text="Say hi", action=cmd("echo hi"))
draw_gui()
```

- [[Draw-GUI]] — windows, buttons and `pos=`
- [[Imports]] — imports work like Python
- [[Changelog]] — what changed in each version
- [[Uninstall]] — taking it all back out
