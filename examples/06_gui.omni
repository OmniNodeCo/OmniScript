# A clickable window, built line by line across statements.
draw_gui.window_size(340, 180, "Greeter")
draw_gui.button(20, 30, 140, 36, "Greet", action=cmd("echo Hello there"))
draw_gui.button(180, 30, 140, 36, "Files", action=cmd("echo listing"))
draw_gui()
