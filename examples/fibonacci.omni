-- Fibonacci in OmniScript

func fib(n) {
    if n <= 0 {
        return 0
    }
    if n == 1 {
        return 1
    }
    return fib(n - 1) + fib(n - 2)
}

show("Fibonacci Sequence:")
loop i in 0..15 {
    show("fib(" + str(i) + ") = " + str(fib(i)))
}